<% @title = "職員アカウントの編集" %>
<h1><%= @title %></h1>

<div>
  <%= form_with model: @customer, url: [ :admin, @customer ] do |f| %>
    <%= render "form", f: f %>
    <div>
      <%= f.submit "更新" %>
      <%= link_to "キャンセル", :admin_customers %>
    </div>
  <% end %>
</div>