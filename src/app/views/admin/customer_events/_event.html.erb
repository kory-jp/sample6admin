
<tr>
  <% unless @customer %>
  <td>
    <%= link_to(event.member.name,
      [ :admin, event.member, :customer_events ]) %>
  </td>
  <% end %>
  <td><%= event.description %></td>
  <td>
    <%= event.occurred_at.strftime("%Y/%m/%d %H:%M:%S") %>
  </td>
</tr>