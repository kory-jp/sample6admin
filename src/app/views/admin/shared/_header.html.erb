<header>
<span>SAMPLE6-app</span>
<%= content_tag(:span, flash.notice) if flash.notice %>
<%= content_tag(:span, flash.alert) if flash.alert %>
  <%=
    if current_administrator
      link_to "ログアウト", :admin_session, method: :delete
    else
      link_to "ログイン", :admin_login
    end
  %>
</header>