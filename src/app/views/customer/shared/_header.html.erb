<header>
  <%= link_to "SAMPLE6-app", :customer_root %>
  <%= content_tag(:span, flash.notice) if flash.notice %>
  <%= content_tag(:span, flash.alert) if flash.alert %>
  <%=
    if current_customer
      link_to "ログアウト", :customer_session, method: :delete
    else
      link_to "ログイン", :customer_login
    end
  %>
  <%= link_to "アカウント", :customer_account if current_customer %>
</header>